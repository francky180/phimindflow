# PHIMINDFLOW Agent System — Documentation

This folder is the docs companion to `agents/`. It holds:
- **ARCHITECTURE.md** — how the agents and workflows compose
- (future) examples, case studies, runbooks

For the actual agent definitions, see `agents/roles/`.
For the workflow specs, see `agents/workflows/`.
For the gates, see `agents/quality-gates/`.
