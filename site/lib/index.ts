// Barrel re-export for the lib/ namespace.
// Purely additive — does not modify any existing import path.
// Enables `import { zernio } from "@/lib"` as an alternative to the
// direct `@/lib/zernio` import.
export { zernio } from "./zernio";
